# MANAGED_BY_ANSIBLE
# shellcheck shell=bash
# --------------------------------

alias       ga='git add'
alias       gb='git branch'
alias      gbr='git branch -r'
alias     gbrv='git branch -rv'
alias     gout='git checkout'
alias       gc='git commit'
alias       gd='git diff'
alias      gdd='git diff .'
alias      gdw='git diff -w'
alias     gdwd='git diff -w .'
alias   gfetch='git fetch origin'
alias   gmerge='git merge'
alias    gpush='git push origin'
alias    gpull='git pull origin'
alias       gr='git remote'
alias      grv='git remote -v'
alias     gruo='git remote update origin'
alias    gruop='git remote update origin --prune'
alias       gs='git status'
alias      gsd='git status .'
alias      gsp='git status --porcelain'
alias     gspd='git status --porcelain .'
alias       gt='git tag'
alias      gtt='git tag | sort -V | tail -8'

# end
